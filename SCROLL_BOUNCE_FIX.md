# 마우스 휠 스크롤 튐 방지 — 조사·해결 기록

> 대상: `init.lua` §[7] `scrollDebouncer`. 작성 2026-07-30.

## 증상

노치(단계식) 마우스 휠로 **아래로 굴리는데 화면이 순간 위로 톡** 튄다. (반대 방향 스크롤에서도 대칭으로 발생.)

## 근본 원인 (측정으로 확정)

`/tmp/hs-scroll2.log`에 실제 이벤트를 타임스탬프와 함께 기록해서 확인:

- 튐 = **한 방향 스트림 한복판에 박히는 고립된 역방향 1틱**. 예: `+` 연속 스트림 속 `dA1=-3`(pt -33 ≈ 33px 점프) 하나. 직전 정방향 틱 ~4~30ms 뒤에 옴.
- 드물게 2틱 버스트도 있음 (`-1 -1`이 4ms 간격 — 사람 손가락 불가 속도).
- **진짜 방향 반전**은 500ms~3초 쉰 뒤 + 여러 틱 연속으로 옴.
- 즉 활성 stream 안에서는 **튐 = 짧은 역방향 run, 반전 = 긴 run**이 유일한 판별자다. 150ms 이상 명확한 휴지는 별도 제스처 경계로 쓸 수 있다.

## v5 갱신 (2026-08-14) — gap-gating (현행)

v4의 튐 억제·누적-방출은 유지하고, 마지막 휠 틱 이후 **150ms 이상 멈춘 뒤 시작한 역방향**만 의도적 반전으로 즉시 확정(`gapFlip`)한다.

- 150ms 미만 밀착 역방향 → 기존 `flipTicks=4` 경로(튐 후보 억제).
- 150ms 이상 휴지 후 역방향 → 첫 틱부터 원본 delta 통과(반전 지연·몰아 방출 없음).
- gap 직전 억제 버퍼 → 새 제스처에 섞이지 않도록 폐기.
- 근거: 실측 튐 진입 gap 26~57ms, 멈춘 뒤 의도적 반전 250ms+. 정확도 향상이 아니라 **명확한 휴지 반전의 지연 제거**가 목적이다.
- 즉발 반전은 여전히 v4의 4틱 확인을 거친다. 마우스 교체만이 하드웨어 튐의 원천 해결이다.

배포 콜백은 `scrollProcessDeltas(...[, nowSeconds])`를 그대로 사용한다. `nowSeconds`를 주입하면 결정뿐 아니라 누적·방출·stale 버퍼 폐기까지 합성 event post 없이 결정론적으로 검증할 수 있다.

## v4 갱신 (2026-08-14) — 통합 run 카운터 + 누적-방출 (v5 기반)

> **현행 로직은 v4 위에 v5 gap-gating을 추가했다. 아래 v1~v4는 도달 경로 기록.**

### 재조사로 뒤집힌 것 (v2는 틀린 접근이었음)

v1(flipTicks=2)이 **2틱 이상 튐 버스트**에 누수해서, 처음엔 **v2(가속-서명)** — "튐은 magnitude≥2 청크, 진짜 반전은 저속 [1,1] 시작" 가정 — 으로 고쳤다. **이 가정은 틀렸다.** 260틱 fp-trace 실측 결과:

- 튐과 진짜 반전은 **magnitude·fp·활성-stream 틱 간격 어느 축으로도 안 갈린다**(분포 전 구간 겹침). 예: 진짜 저속반전 첫틱 `fp=0.10` = mag-1 튐 첫틱 `fp=0.10` (동일). 청크 튐 첫틱 `fp≈3` = 진짜 빠른반전 첫틱 `fp≈3` (동일).
- v2가 놓친 실누수: down 스크롤 중 `+1,+1,+3`(저속 시작 튐)이 rampTicks=2로 flip돼 통과 = 화면 "위로 톡".
- **유일하게 신뢰할 판별자는 run 길이(지속)뿐이고, 그것도 연속 분포**(1~52틱, 깔끔한 간격 없음). 이 마우스 튐은 최대 4틱까지 옴(문서 초기 ≤2 가정 초과).

### v3 — 통합 run 카운터 (magnitude 폐기)

역방향 틱을 magnitude 무관하게 세어, 연속 `flipTicks(=4)` 모이기 전엔 튐으로 억제, 이상이면 진짜 반전으로 방향 전환. `flipTicks=4` = ≤3틱 튐 억제(대다수), 잔여 = 드문 4틱 튐. **트레이드오프**: 진짜 반전 첫 (flipTicks-1)=3틱 손실.

### v4 — 누적-방출 (반전 거리 무손실)

v3의 "억제=delta 0화"는 진짜 반전의 첫 3틱을 **영구 손실**시켰다(반전 시 3노치 씹힘). v4는 억제 틱의 delta를 **버퍼에 누적**했다가:
- 튐이 원방향 복귀(pass) → 버퍼 폐기(움직임 0, 튐 흡수).
- 반전 확정(flip) → 버퍼를 그 틱에 몰아 방출 → **거리 무손실**.

**트레이드오프**: 즉발 반전은 손실 대신 반전 순간 "톡 튀며 따라잡는" 저크(최대 3틱분). 150ms 이상 휴지 반전은 v5가 이 지연을 제거한다.

### 근본 한계 (정직)

causal 필터(미래 못 봄)는 "지속"을 확인하려면 틱을 흘려야 하고, 흘린 게 튐이면 그게 보이는 튐 → **튐 억제 ↔ 반전 지연은 물리적으로 맞바꿀 수밖에 없다.** 완벽 분리는 소프트웨어로 불가(측정 확인). 튐은 마우스 엔코더의 스퓨리어스 역틱(하드웨어 결함, 근거: 4ms 간격 역틱=사람 불가 속도) — **진짜 원천 해결은 마우스 교체.**

### 검증 방법 (재현 가능)

이 환경에선 `newScrollEvent:post()`가 eventtap에 도달 안 함(프로브 실측 6 post→0) → 이벤트 주입 검증 불가. 그래서:
- 결정 로직 `scrollDecide(dir[,mag[,gapMs]])`와 배포 delta 로직 `scrollProcessDeltas(line,fp,pt[,nowSeconds])`를 노출 → `scrollDecideReset()` 후 직접 호출 = 결정·버퍼의 결정론 오라클.
- 합성 하네스: `~/.hammerspoon/scroll_harness.py` (방향·gap 시퀀스 + 결정·버퍼 시뮬).
- 자동 시나리오: 밀착 3틱 튐 억제, 밀착 반전 누적-방출, 휴지 반전 즉시 통과, stale 버퍼 폐기, 150ms 미만 v4 유지.
- 실제 화면 체감은 합성 post 제약 때문에 여전히 실 마우스 스크롤로 최종 판정한다.

---

## v1 해결 원리 (3가지) — 아래는 원본 flipTicks 로직 (v4가 대체)

## 해결 원리 (3가지)

1. **확정 방향(committedDir)을 기억한다.** 현재 스크롤 방향을 붙잡고 있는다.
2. **역방향 틱이 연속 `flipTicks`(=2)개 모이기 전엔 튐**으로 보고 억제, 2개 이상 연속이면 진짜 반전으로 방향 전환. 같은 방향 틱이 오면 카운트 리셋(→ 고립 1틱 튐은 다음 정방향 틱에서 리셋되며 흡수).
3. **억제는 삭제가 아니라 `setProperty`로 delta=0.** 이 환경에선 `return true`(이벤트 삭제)가 스크롤을 못 막았다. `setProperty`(값 수정)만 먹혔다. 그래서 튐 틱의 delta를 0으로 덮어써 움직임을 없앤다. (재부호가 아니라 0화 — 진짜 반전의 첫 틱이 옛 방향으로 헛구르는 걸 방지.)

**시간창을 쓰지 않는다.** 빠른 반전과 튐 버스트를 시간으로 못 가르는 게 실패의 핵심 원인이었다. 개수만 본다 → 빠르든 느리든 무관.

## 튜닝

- `flipTicks`(기본 2): 방향 전환에 필요한 연속 역방향 틱 수.
  - 튐이 2틱씩 새면 → 3으로 올린다 (대신 진짜 반전이 첫 2틱 죽음).
  - 진짜 반전의 첫 1틱 죽는 게 거슬려도 최소값이 2라 그대로 둔다.
- 트랙패드(연속 pixel scroll)는 건드리지 않는다 — 양방향 제스처가 정상이므로.
- 진단 카운터 `scrollStats {passed, droppedBounce, flips}` 는 동작 무관, 튜닝 확인용으로 남겨둠.
  - 조회: `osascript -e 'tell application "Hammerspoon" to execute lua code "return scrollStats.droppedBounce"'`

## 실패 이력 (7시도) — 재발 방지용

| 시도 | 방법 | 실패 원인 |
|------|------|----------|
| 1~3 | 실제 이벤트 안 보고 추측 코딩 | 튐의 정체(1틱/magnitude/연속여부)를 모른 채 heuristic만 조합 |
| 4 | 시간창 debounce (80ms) | gap 뒤에 오는 튐이 시간창을 빠져나감 |
| 5 | 제스처 최초 5틱 평균으로 방향 결정 | 제스처 경계(idle-gap) 의존 → 경계 밖 튐 누수 |
| 6 | 연속 역방향 카운트 + `return true` 삭제 | **삭제가 이 환경서 무효** (16틱 "지웠는데" 그대로 튐) |
| 7a | 시간창 + committed 방향 재부호(setProperty) | 재부호 시 시계 갱신 → 빠른 진짜 반전이 영영 옛 방향으로 재부호됨 (방향 특정 1/3 실패) |
| **7b (성공)** | **연속 카운트 + delta 0화(setProperty)** | 시간 안 봄 + 삭제 대신 먹히는 0화 사용 |

## 메타 교훈 (조사 방법)

- **추측 패치 금지, 측정 먼저.** 실제 이벤트 로그를 보고서야 "튐=1틱, magnitude 3, 비연속"을 알았다. 그 전 3시도는 전부 헛수고.
- **프록시가 아니라 진짜 증상으로 검증.** "카운터 16개 억제"에 속아 성공으로 착각했으나 화면은 그대로였다(삭제 무효 때문). 눈으로 본 "위로 톡"이 유일한 판정 기준.
- **토대 가정을 먼저 굳혀라.** "이 eventtap이 이 마우스 스크롤을 실제로 통제할 수 있나?"(삭제 vs setProperty)를 안 굳히고 위층 heuristic만 바꿔 6번 헛돌았다.

## Hammerspoon 리로드 주의

세션 중 `osascript`로 런타임에 주입한 전역(임시 로거 등)은 init.lua가 리로드되면 사라진다. 그래서 진단 코드도 init.lua 안에 심어야 리로드에도 살아남는다. 최종 로직은 init.lua §[7]에 영속.
